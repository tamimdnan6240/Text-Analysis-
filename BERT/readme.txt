Created a .py file so that I can just run the script. 

Open cmd at the Anaconda

cd "paste your folder directory" 

type -- dir -- commmand to see the files in your directory

Now just type -- Python TEST_Abstracts_With_BERT.py 
